#!/bin/bash
# vim: set ft=sh:
# Rebuild the bat/batcat cache so the custom Moss theme registers.
# Re-runs when any theme is added, removed, or changed (hashes below trigger it).
# themes:{{ range (glob (joinPath .chezmoi.sourceDir "dot_config/batcat/themes/*")) }} {{ include . | sha256sum }}{{ end }}
{{ .cmds.bat }} cache --build
