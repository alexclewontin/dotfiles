source ~/.profile
source ~/.path
source ~/.aliases

# GPG: ensure gpg-agent uses the current TTY, and signal TUI pinentry for SSH
export GPG_TTY=$(tty)
if [ -n "$SSH_CONNECTION" ]; then
    export PINENTRY_USER_DATA="USE_CURSES=1"
fi
gpg-connect-agent updatestartuptty /bye > /dev/null 2>&1
