
**Behaviors**:
    When I report a bug, before diagnosing and fixing, you should first create a unit test that reproduces it. Then diagnose and fix, if appropriate.
**Tools**: 
    - `cat` is aliased to `bat` 
    - `ls` is aliased to `eza`
    - `cd` is aliased `cd $@ && pwd && ls`
    - `tree` is installed

